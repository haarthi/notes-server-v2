# Path to the notes home directory. Tests override via fixtures.
# For dev, point at a wipeable folder rather than your real ~/Notes/.
NOTES_HOME=./dev-notes

# Server bind
HOST=localhost
PORT=8765

# Logging
LOG_LEVEL=INFO
